[project]
name = "things3-enhanced-mcp"
version = "2.1.0"
description = "Production-ready Model Context Protocol server for Things 3, enabling AI assistants to interact with your task management system through natural language"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT License" }
authors = [
    { name = "Yaroslav Krempovych", email = "51231325+excelsior@users.noreply.github.com" },
    { name = "Harald Lindstrøm", email = "hald@users.noreply.github.com" },
    { name = "Jan Kampling", email = "kmplngj@users.noreply.github.com" }
    ]
keywords = [
    "mcp",
    "anthropic",
    "claude",
    "things3",
    "things",
    "task-management",
    "productivity",
    "fastmcp",
    "macos",
    "apple",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: End Users/Desktop",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.12",
    "Topic :: Utilities",
    "Topic :: Office/Business :: Scheduling",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Operating System :: MacOS",
]
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.2.0",
    "fastmcp>=2.9.0",
    "python-dotenv>=1.0.0",
    "things-py>=0.0.15",
    "rich>=13.7.0",
    "dateparser>=1.2.0",
    "py-key-value-aio[disk]>=0.2.8",
]

[project.urls]
"Homepage" = "https://github.com/kmplngj/things-fastmcp"
"Bug Tracker" = "https://github.com/kmplngj/things-fastmcp/issues"
"Documentation" = "https://github.com/kmplngj/things-fastmcp#readme"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/things_mcp"]

[tool.hatch.build.targets.sdist]
include = [
    "src/things_mcp",
    "README.md",
    "LICENSE",
    "pyproject.toml",
    "things_fast_server.py",
]

[tool.hatch.envs.default]
dependencies = ["pytest", "pytest-cov", "ruff", "build", "twine"]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "ruff>=0.1.0",
    "build>=0.10.0",
    "twine>=4.0.0",
]

[project.scripts]
server = "things_mcp.things_fast_server:main"
dev = "things_mcp.things_fast_server:main"

[tool.hatch.envs.default.scripts]
test = "pytest {args:tests}"
lint = "ruff check ."
format = "ruff format ."
build = "python -m build"
publish = "twine upload dist/*"
clean = "rm -rf dist build *.egg-info"

[tool.pyright]
# FastMCP automatically handles CallToolResult return types
# even when tools are annotated as returning str
reportReturnType = false
